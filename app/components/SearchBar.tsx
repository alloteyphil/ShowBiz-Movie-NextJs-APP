import { SearchIcon } from "lucide-react";

const SearchBar = () => {
  return (
    <div>
      <SearchIcon size={22} className="cursor-pointer" color="#fff" />
    </div>
  );
};

export default SearchBar;
